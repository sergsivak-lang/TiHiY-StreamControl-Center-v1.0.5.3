<Window x:Class="TiHiY.StreamControlCenter.Windows.LocalChatOverlayWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:models="clr-namespace:TiHiY.StreamControlCenter.Models"
        xmlns:controls="clr-namespace:TiHiY.StreamControlCenter.Controls"
        Title="TiHiY Local Chat Overlay" Width="620" Height="420" MinWidth="360" MinHeight="220"
        WindowStyle="None" ResizeMode="CanResizeWithGrip" AllowsTransparency="True"
        Background="Transparent" Topmost="True" ShowInTaskbar="False"
        WindowStartupLocation="CenterScreen" SourceInitialized="Window_SourceInitialized"
        Loaded="Window_Loaded" Closing="Window_Closing">
    <Window.Resources>
        <SolidColorBrush x:Key="LocalOverlayTextBrush" Color="#F2FAFF"/>
        <SolidColorBrush x:Key="LocalOverlayUserBrush" Color="#55C8FF"/>
        <Geometry x:Key="YouTubePlayGeometry">M 0,0 L 0,10 L 9,5 Z</Geometry>
    </Window.Resources>
    <Border x:Name="OverlayFrame" Background="#22000000" BorderBrush="#3D8CA0" BorderThickness="1" CornerRadius="8" Padding="8">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <Grid x:Name="ControlBar" Margin="2,0,2,6">
                <Grid.ColumnDefinitions><ColumnDefinition/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
                <Border Background="Transparent" MouseLeftButtonDown="Window_MouseLeftButtonDown">
                    <TextBlock Text="ЧАТ ПОВЕРХ ГРИ • ВИДНО ТІЛЬКИ ВАМ" Foreground="#46D8FF" FontWeight="Bold" FontSize="12" VerticalAlignment="Center"/>
                </Border>
                <StackPanel Grid.Column="1" Orientation="Horizontal">
                    <TextBlock x:Name="ModeText" Text="КЕРУВАННЯ" Foreground="{DynamicResource Muted}" FontSize="10" VerticalAlignment="Center" Margin="0,0,8,0"/>
                    <Button Style="{StaticResource WindowButton}" Content="×" Width="30" Height="24" Padding="0" Margin="0" Click="Close_Click"/>
                </StackPanel>
            </Grid>

            <Border x:Name="ViewerStatsBar" Grid.Row="1" Background="#26030B10" BorderBrush="{DynamicResource LineSoft}" BorderThickness="0,0,0,1" Padding="5,3" Margin="0,0,0,5">
                <Grid>
                    <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="1"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions>
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">
                        <Image x:Name="TwitchOverlayIcon" Source="/TiHiY.StreamControlCenter;component/Assets/Platforms/twitch.png" Width="18" Height="18" Stretch="Uniform"/>
                        <TextBlock Text="  ГЛЯДАЧІ " Foreground="{DynamicResource Muted}" VerticalAlignment="Center"/>
                        <TextBlock x:Name="TwitchOverlayViewersText" Text="0" Foreground="{DynamicResource Text}" FontWeight="Bold" VerticalAlignment="Center"/>
                        <Ellipse x:Name="TwitchOverlayLiveDot" Width="7" Height="7" Fill="#75838B" Margin="9,0,4,0"/>
                        <TextBlock x:Name="TwitchOverlayStatusText" Text="OFF" Foreground="{DynamicResource Muted}" FontWeight="Bold" VerticalAlignment="Center"/>
                    </StackPanel>
                    <Border Grid.Column="1" Background="#355B6B"/>
                    <StackPanel Grid.Column="2" Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">
                        <Grid x:Name="YouTubeOverlayIcon" Width="22" Height="18" Margin="0">
                            <Border Background="#FF0000" CornerRadius="4" Width="22" Height="15" VerticalAlignment="Center"/>
                            <Path Data="{StaticResource YouTubePlayGeometry}" Fill="White" Width="8" Height="9" Stretch="Fill" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Grid>
                        <TextBlock Text="  ГЛЯДАЧІ " Foreground="{DynamicResource Muted}" VerticalAlignment="Center"/>
                        <TextBlock x:Name="YouTubeOverlayViewersText" Text="0" Foreground="{DynamicResource Text}" FontWeight="Bold" VerticalAlignment="Center"/>
                        <TextBlock Text="   ♥ " Foreground="#FF6B6B" FontWeight="Bold" VerticalAlignment="Center"/>
                        <TextBlock x:Name="YouTubeOverlayLikesText" Text="0" Foreground="{DynamicResource Text}" FontWeight="Bold" VerticalAlignment="Center"/>
                        <Ellipse x:Name="YouTubeOverlayLiveDot" Width="7" Height="7" Fill="#75838B" Margin="9,0,4,0"/>
                        <TextBlock x:Name="YouTubeOverlayStatusText" Text="OFF" Foreground="{DynamicResource Muted}" FontWeight="Bold" VerticalAlignment="Center"/>
                    </StackPanel>
                </Grid>
            </Border>

            <ListBox x:Name="OverlayChatList" Grid.Row="2" ItemsSource="{Binding Messages}"
                     Background="Transparent" BorderThickness="0" Padding="0"
                     ScrollViewer.VerticalScrollBarVisibility="Hidden"
                     ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                     IsHitTestVisible="False">
                <ListBox.ItemContainerStyle>
                    <Style TargetType="ListBoxItem">
                        <Setter Property="Focusable" Value="False"/>
                        <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                        <Setter Property="Padding" Value="0"/>
                        <Setter Property="Margin" Value="0,0,0,6"/>
                        <Setter Property="Background" Value="Transparent"/>
                        <Setter Property="BorderThickness" Value="0"/>
                    </Style>
                </ListBox.ItemContainerStyle>
                <ListBox.ItemTemplate>
                    <DataTemplate DataType="{x:Type models:ChatMessage}">
                        <Border Background="Transparent" CornerRadius="4" Padding="6,4">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="30"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <Grid Width="24" Height="22" VerticalAlignment="Top">
                                    <Border x:Name="PlatformIconBackground" CornerRadius="4" Background="{Binding PlatformColor}"/>
                                    <Image x:Name="PlatformRasterIcon" Source="{Binding PlatformIconPath}" Stretch="Uniform" Margin="2"/>
                                    <Grid x:Name="YouTubeVectorIcon" Visibility="Collapsed" Margin="2">
                                        <Border Background="#FF0000" CornerRadius="3" VerticalAlignment="Center" Height="14"/>
                                        <Path Data="{StaticResource YouTubePlayGeometry}" Fill="White" Width="7" Height="8" Stretch="Fill" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                    </Grid>
                                </Grid>
                                <controls:RichChatTextBlock Grid.Column="1" Margin="6,0,0,0"
                                                            Message="{Binding}" IncludeUser="True"
                                                            UserBrush="{DynamicResource LocalOverlayUserBrush}"
                                                            MessageBrush="{DynamicResource LocalOverlayTextBrush}"
                                                            HighlightBrush="#FFD329" EmoteSize="24"/>
                            </Grid>
                        </Border>
                        <DataTemplate.Triggers>
                            <DataTrigger Binding="{Binding Platform}" Value="YOUTUBE">
                                <Setter TargetName="PlatformRasterIcon" Property="Visibility" Value="Collapsed"/>
                                <Setter TargetName="PlatformIconBackground" Property="Background" Value="Transparent"/>
                                <Setter TargetName="YouTubeVectorIcon" Property="Visibility" Value="Visible"/>
                            </DataTrigger>
                        </DataTemplate.Triggers>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </Grid>
    </Border>
</Window>