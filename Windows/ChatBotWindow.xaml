<services:ModuleWindowBase xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:services="clr-namespace:TiHiY.StreamControlCenter.Services" xmlns:models="clr-namespace:TiHiY.StreamControlCenter.Models" x:Class="TiHiY.StreamControlCenter.Windows.ChatBotWindow" Title="Чат і бот" Width="1280" Height="800" MinWidth="650" MinHeight="410" WindowStyle="None" ResizeMode="CanResize">
    <Border BorderBrush="{DynamicResource Line}" BorderThickness="1" Background="{DynamicResource WindowGradient}">
            <Grid x:Name="DesignSurface" Margin="12">
                <Grid.RowDefinitions><RowDefinition Height="56"/><RowDefinition Height="52"/><RowDefinition/></Grid.RowDefinitions>
                <Border Grid.Row="0" Background="{DynamicResource Panel2}" BorderBrush="{DynamicResource Line}" BorderThickness="0,0,0,1" MouseLeftButtonDown="TitleBar_MouseLeftButtonDown">
                    <Grid Margin="14,0"><Grid.ColumnDefinitions><ColumnDefinition/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center"><TextBlock Text="CHAT CORE" FontSize="22" FontWeight="Bold" Foreground="{DynamicResource Cyan}"/><TextBlock Text="  •  TWITCH + YOUTUBE + BOT" VerticalAlignment="Center" Foreground="{DynamicResource Muted}"/></StackPanel>
                        <StackPanel Grid.Column="1" Orientation="Horizontal"><Button Style="{StaticResource WindowButton}" Content="—" Width="40" Height="32" Padding="0" Click="Minimize_Click"/><Button Style="{StaticResource WindowButton}" Content="□" Width="40" Height="32" Padding="0" Click="Maximize_Click"/><Button Style="{StaticResource WindowButton}" Content="×" Width="40" Height="32" Padding="0" Background="#3B0D13" BorderBrush="{DynamicResource Red}" Click="Close_Click"/></StackPanel>
                    </Grid>
                </Border>
                <Border Grid.Row="1" Background="{DynamicResource Panel}" BorderBrush="{DynamicResource Line}" BorderThickness="0,0,0,1">
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center" HorizontalAlignment="Center">
                        <Button Content="ОБ’ЄДНАНИЙ ЧАТ" Click="ShowChat_Click" Background="#1453A5"/>
                        <Button Content="АВТОСПОВІЩЕННЯ" Click="ShowNotices_Click"/>
                        <Button Content="КОМАНДИ БОТА" Click="ShowCommands_Click"/>
                    </StackPanel>
                </Border>

                <Grid Grid.Row="2" x:Name="ChatPanel" Margin="0,12,0,0">
                    <Grid.ColumnDefinitions><ColumnDefinition Width="2.2*"/><ColumnDefinition Width="1*"/></Grid.ColumnDefinitions>
                    <Border Style="{StaticResource PanelBorder}" Margin="0,0,8,0">
                        <Grid><Grid.RowDefinitions><RowDefinition Height="36"/><RowDefinition/><RowDefinition Height="90"/></Grid.RowDefinitions>
                            <Grid><TextBlock Text="ПОВІДОМЛЕННЯ" FontSize="18" FontWeight="Bold" Foreground="{DynamicResource Cyan}"/><TextBlock Text="Колір тексту залежить від статусу, звернення виділяються" HorizontalAlignment="Right" Foreground="{DynamicResource Muted}"/></Grid>
                            <ListBox x:Name="ChatMessagesList" Grid.Row="1" ItemsSource="{Binding VisibleMessages}" Background="Transparent" BorderThickness="0" Padding="0" ScrollViewer.VerticalScrollBarVisibility="Auto" ScrollViewer.HorizontalScrollBarVisibility="Disabled" VirtualizingPanel.IsVirtualizing="True" VirtualizingPanel.VirtualizationMode="Recycling" PreviewMouseRightButtonDown="ChatMessagesList_PreviewMouseRightButtonDown">
                                <ListBox.ItemContainerStyle>
                                    <Style TargetType="ListBoxItem">
                                        <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                                        <Setter Property="Padding" Value="0"/><Setter Property="Margin" Value="0"/>
                                        <Setter Property="Background" Value="Transparent"/><Setter Property="BorderThickness" Value="0"/>
                                    </Style>
                                </ListBox.ItemContainerStyle>
                                <ListBox.ItemTemplate><DataTemplate DataType="{x:Type models:ChatMessage}">
                                    <Border Background="{Binding Background}" BorderBrush="{DynamicResource LineSoft}" BorderThickness="0,0,0,1" Padding="7,5">
                                        <Grid><Grid.ColumnDefinitions><ColumnDefinition Width="62"/><ColumnDefinition Width="42"/><ColumnDefinition Width="140"/><ColumnDefinition/></Grid.ColumnDefinitions>
                                            <TextBlock Text="{Binding DisplayTime}" Foreground="{DynamicResource Muted}" FontSize="11"/>
                                            <Border Grid.Column="1" Width="27" Height="22" CornerRadius="4" Background="{Binding PlatformColor}" HorizontalAlignment="Left"><Image Source="{Binding PlatformIconPath}" Stretch="Uniform" Margin="2"/></Border>
                                            <TextBlock Grid.Column="2" Text="{Binding User}" Foreground="{Binding Foreground}" FontWeight="Bold" TextTrimming="CharacterEllipsis"/>
                                            <TextBlock Grid.Column="3" Text="{Binding Text}" Foreground="{Binding Foreground}" TextWrapping="Wrap"/>
                                        </Grid>
                                    </Border>
                                </DataTemplate></ListBox.ItemTemplate>
                            </ListBox>
                            <Grid Grid.Row="2" Margin="0,8,0,0"><Grid.RowDefinitions><RowDefinition/><RowDefinition Height="42"/></Grid.RowDefinitions>
                                <TextBox x:Name="ManualMessageTextBox" AcceptsReturn="True" TextWrapping="Wrap"/>
                                <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Right">
                                    <Button Click="SendTwitch_Click" Background="#563A8A"><StackPanel Orientation="Horizontal"><Image Source="/TiHiY.StreamControlCenter;component/Assets/Platforms/twitch.png" Width="18" Height="18" Stretch="Uniform" VerticalAlignment="Center"/><TextBlock Text="  TWITCH" VerticalAlignment="Center"/></StackPanel></Button>
                                    <Button Click="SendYouTube_Click" Background="#8A2027"><StackPanel Orientation="Horizontal"><Image Source="/TiHiY.StreamControlCenter;component/Assets/Platforms/youtube.png" Width="18" Height="18" Stretch="Uniform" VerticalAlignment="Center"/><TextBlock Text="  YOUTUBE" VerticalAlignment="Center"/></StackPanel></Button>
                                    <Button Content="В ОБИДВІ" Click="SendBoth_Click" Background="#1453A5"/>
                                </StackPanel>
                            </Grid>
                        </Grid>
                    </Border>
                    <Border Grid.Column="1" Style="{StaticResource PanelBorder}" Margin="8,0,0,0">
                        <Grid><Grid.RowDefinitions><RowDefinition Height="36"/><RowDefinition/><RowDefinition Height="44"/></Grid.RowDefinitions>
                            <TextBlock Text="ДІАГНОСТИКА ЧАТУ" FontSize="18" FontWeight="Bold" Foreground="{DynamicResource Cyan}"/>
                            <StackPanel Grid.Row="1" VerticalAlignment="Center">
                                <TextBlock Text="Повідомлення надсилаються у реально підключені Twitch і YouTube чати. Авторизація виконується у модулі «Канали та Discord», токени не зберігаються у відкритому файлі." TextWrapping="Wrap" Foreground="{DynamicResource Muted}"/>
                                <Button Content="ДОДАТИ ЗВЕРНЕННЯ ДО TiHiY-DED" Click="AddMention_Click" Margin="4,20,4,4"/>
                                <Button Content="ДОДАТИ ПІДПИСНИКА" Click="AddSubscriber_Click"/>
                                <Button Content="ДОДАТИ МОДЕРАТОРА" Click="AddModerator_Click"/>
                            </StackPanel>
                            <TextBlock Grid.Row="2" Text="Вікно чату працює незалежно від головного вікна" Foreground="{DynamicResource Green}" VerticalAlignment="Center" TextWrapping="Wrap"/>
                        </Grid>
                    </Border>
                </Grid>

                <Grid Grid.Row="2" x:Name="NoticesPanel" Visibility="Collapsed" Margin="0,12,0,0">
                    <Grid.ColumnDefinitions><ColumnDefinition Width="1.4*"/><ColumnDefinition Width="1*"/></Grid.ColumnDefinitions>
                    <Border Style="{StaticResource PanelBorder}" Margin="0,0,8,0">
                        <Grid><Grid.RowDefinitions><RowDefinition Height="42"/><RowDefinition/><RowDefinition Height="44"/></Grid.RowDefinitions>
                            <Grid><TextBlock Text="АВТОСПОВІЩЕННЯ ЗА ТАЙМЕРОМ" FontSize="18" FontWeight="Bold" Foreground="{DynamicResource Cyan}"/><CheckBox x:Name="AutoNoticesCheck" Content="Увімкнені" HorizontalAlignment="Right" Checked="AutoNoticesCheck_Changed" Unchecked="AutoNoticesCheck_Changed"/></Grid>
                            <ItemsControl Grid.Row="1" ItemsSource="{Binding NoticePage}">
                                <ItemsControl.ItemsPanel><ItemsPanelTemplate><UniformGrid Rows="5" Columns="1"/></ItemsPanelTemplate></ItemsControl.ItemsPanel>
                                <ItemsControl.ItemTemplate><DataTemplate DataType="{x:Type models:ScheduledNotice}">
                                    <Button Tag="{Binding}" Click="SelectNotice_Click" HorizontalAlignment="Stretch" HorizontalContentAlignment="Stretch" Padding="10" Margin="3">
                                        <Grid><Grid.ColumnDefinitions><ColumnDefinition Width="150"/><ColumnDefinition/><ColumnDefinition Width="120"/><ColumnDefinition Width="82"/></Grid.ColumnDefinitions>
                                            <TextBlock Text="{Binding Name}" FontWeight="Bold" TextTrimming="CharacterEllipsis"/>
                                            <TextBlock Grid.Column="1" Text="{Binding Text}" Foreground="{DynamicResource Muted}" TextTrimming="CharacterEllipsis" Margin="8,0"/>
                                            <TextBlock Grid.Column="2" Text="{Binding Target}" Foreground="{DynamicResource Cyan}"/>
                                            <TextBlock Grid.Column="3" Text="{Binding NextRunText}" Foreground="{DynamicResource Green}" HorizontalAlignment="Right"/>
                                        </Grid>
                                    </Button>
                                </DataTemplate></ItemsControl.ItemTemplate>
                            </ItemsControl>
                            <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Center"><Button Content="‹" Width="44" Click="PreviousNoticePage_Click"/><TextBlock x:Name="NoticePageText" Text="1 / 1" VerticalAlignment="Center" Margin="12,0"/><Button Content="›" Width="44" Click="NextNoticePage_Click"/></StackPanel>
                        </Grid>
                    </Border>
                    <Border Grid.Column="1" Style="{StaticResource PanelBorder}" Margin="8,0,0,0">
                        <Grid><Grid.RowDefinitions><RowDefinition Height="36"/><RowDefinition Height="42"/><RowDefinition/><RowDefinition Height="42"/><RowDefinition Height="54"/></Grid.RowDefinitions>
                            <TextBlock Text="РЕДАКТОР СПОВІЩЕННЯ" FontSize="18" FontWeight="Bold" Foreground="{DynamicResource Cyan}"/>
                            <Grid Grid.Row="1"><Grid.ColumnDefinitions><ColumnDefinition/><ColumnDefinition Width="170"/></Grid.ColumnDefinitions><TextBox x:Name="NoticeNameBox" ToolTip="Назва"/><ComboBox x:Name="NoticeTargetCombo" Grid.Column="1" SelectedIndex="2" Margin="6,0,0,0"><ComboBoxItem Content="Twitch"/><ComboBoxItem Content="YouTube"/><ComboBoxItem Content="Twitch + YouTube"/></ComboBox></Grid>
                            <TextBox x:Name="NoticeTextBox" Grid.Row="2" AcceptsReturn="True" TextWrapping="Wrap" Margin="0,8"/>
                            <Grid Grid.Row="3"><Grid.ColumnDefinitions><ColumnDefinition/><ColumnDefinition/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions><StackPanel Orientation="Horizontal"><TextBlock Text="Кожні, хв:" VerticalAlignment="Center"/><TextBox x:Name="NoticeIntervalBox" Text="20" Width="54" Margin="6,0"/></StackPanel><StackPanel Grid.Column="1" Orientation="Horizontal"><TextBlock Text="Мін. повідомлень:" VerticalAlignment="Center"/><TextBox x:Name="NoticeMinMessagesBox" Text="0" Width="54" Margin="6,0"/></StackPanel><CheckBox x:Name="NoticeEnabledCheck" Grid.Column="2" Content="Увімкнено" IsChecked="True"/></Grid>
                            <WrapPanel Grid.Row="4" HorizontalAlignment="Right"><Button Content="НОВЕ" Click="NewNotice_Click"/><Button Content="ЗБЕРЕГТИ" Click="SaveNotice_Click" Background="#123B32"/><Button Content="НАДІСЛАТИ ЗАРАЗ" Click="SendNoticeNow_Click" Background="#1453A5"/><Button Content="ВИДАЛИТИ" Click="DeleteNotice_Click" Background="#7C2027"/></WrapPanel>
                        </Grid>
                    </Border>
                </Grid>

                <Grid Grid.Row="2" x:Name="CommandsPanel" Visibility="Collapsed" Margin="0,12,0,0">
                    <Grid.ColumnDefinitions><ColumnDefinition Width="1.4*"/><ColumnDefinition Width="1*"/></Grid.ColumnDefinitions>
                    <Border Style="{StaticResource PanelBorder}" Margin="0,0,8,0">
                        <Grid><Grid.RowDefinitions><RowDefinition Height="42"/><RowDefinition/><RowDefinition Height="44"/></Grid.RowDefinitions>
                            <TextBlock Text="КОМАНДИ БОТА" FontSize="18" FontWeight="Bold" Foreground="{DynamicResource Cyan}"/>
                            <ItemsControl Grid.Row="1" ItemsSource="{Binding CommandPage}">
                                <ItemsControl.ItemsPanel><ItemsPanelTemplate><UniformGrid Rows="6" Columns="1"/></ItemsPanelTemplate></ItemsControl.ItemsPanel>
                                <ItemsControl.ItemTemplate><DataTemplate DataType="{x:Type models:BotCommand}"><Button Tag="{Binding}" Click="SelectCommand_Click" HorizontalAlignment="Stretch" HorizontalContentAlignment="Stretch" Padding="10" Margin="3"><Grid><Grid.ColumnDefinitions><ColumnDefinition Width="120"/><ColumnDefinition/><ColumnDefinition Width="140"/><ColumnDefinition Width="90"/></Grid.ColumnDefinitions><TextBlock Text="{Binding Name}" FontWeight="Bold" Foreground="{DynamicResource Green}"/><TextBlock Grid.Column="1" Text="{Binding Reply}" TextTrimming="CharacterEllipsis" Margin="8,0"/><TextBlock Grid.Column="2" Text="{Binding Target}" Foreground="{DynamicResource Cyan}"/><TextBlock Grid.Column="3" Text="{Binding CooldownText}" HorizontalAlignment="Right"/></Grid></Button></DataTemplate></ItemsControl.ItemTemplate>
                            </ItemsControl>
                            <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Center"><Button Content="‹" Width="44" Click="PreviousCommandPage_Click"/><TextBlock x:Name="CommandPageText" Text="1 / 1" VerticalAlignment="Center" Margin="12,0"/><Button Content="›" Width="44" Click="NextCommandPage_Click"/></StackPanel>
                        </Grid>
                    </Border>
                    <Border Grid.Column="1" Style="{StaticResource PanelBorder}" Margin="8,0,0,0">
                        <Grid><Grid.RowDefinitions><RowDefinition Height="36"/><RowDefinition Height="42"/><RowDefinition/><RowDefinition Height="42"/><RowDefinition Height="54"/></Grid.RowDefinitions>
                            <TextBlock Text="РЕДАКТОР КОМАНДИ" FontSize="18" FontWeight="Bold" Foreground="{DynamicResource Cyan}"/>
                            <Grid Grid.Row="1"><Grid.ColumnDefinitions><ColumnDefinition/><ColumnDefinition Width="170"/></Grid.ColumnDefinitions><TextBox x:Name="CommandNameBox" Text="!команда"/><ComboBox x:Name="CommandTargetCombo" Grid.Column="1" SelectedIndex="2" Margin="6,0,0,0"><ComboBoxItem Content="Twitch"/><ComboBoxItem Content="YouTube"/><ComboBoxItem Content="Twitch + YouTube"/></ComboBox></Grid>
                            <TextBox x:Name="CommandReplyBox" Grid.Row="2" AcceptsReturn="True" TextWrapping="Wrap" Margin="0,8"/>
                            <Grid Grid.Row="3"><Grid.ColumnDefinitions><ColumnDefinition/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions><StackPanel Orientation="Horizontal"><TextBlock Text="Cooldown, сек:" VerticalAlignment="Center"/><TextBox x:Name="CommandCooldownBox" Text="10" Width="60" Margin="6,0"/></StackPanel><CheckBox x:Name="CommandEnabledCheck" Grid.Column="1" Content="Увімкнено" IsChecked="True"/></Grid>
                            <WrapPanel Grid.Row="4" HorizontalAlignment="Right"><Button Content="НОВА" Click="NewCommand_Click"/><Button Content="ЗБЕРЕГТИ" Click="SaveCommand_Click" Background="#123B32"/><Button Content="ТЕСТ" Click="TestCommand_Click" Background="#1453A5"/><Button Content="ВИДАЛИТИ" Click="DeleteCommand_Click" Background="#7C2027"/></WrapPanel>
                        </Grid>
                    </Border>
                </Grid>
            </Grid>
    </Border>
</services:ModuleWindowBase>
